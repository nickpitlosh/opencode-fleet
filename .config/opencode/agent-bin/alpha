#!/usr/bin/env bash
# alpha — runs opencode with opencode-go/union-alpha
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
exec "$HOME/.opencode/bin/opencode" run -m "opencode-go/union-alpha" "$@"
