#!/usr/bin/env bash
# maverick — runs opencode with opencode-go/grok-4.6
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
exec "$HOME/.opencode/bin/opencode" run -m "opencode-go/grok-4.6" "$@"
