#!/usr/bin/env bash
# maxim — runs opencode with opencode-go/minimax-m3
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
exec "$HOME/.opencode/bin/opencode" run -m "opencode-go/minimax-m3" "$@"
