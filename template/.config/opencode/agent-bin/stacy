#!/usr/bin/env bash
# stacy — runs opencode with opencode-go/deepseek-v4-flash
export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
exec "$HOME/.opencode/bin/opencode" run -m "opencode-go/deepseek-v4-flash" "$@"
